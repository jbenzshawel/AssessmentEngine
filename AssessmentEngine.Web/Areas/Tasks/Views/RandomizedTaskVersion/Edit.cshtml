@model TaskVersionViewModel

@{
    ViewData["Title"] = "Edit Randomized Task Version";
}

@Html.EditorForModel()

@section Scripts
{
    <script>
        const viewModel = @Html.Raw(Json.Serialize(Model)); 
        const editView = new TaskVersionView(viewModel, @Html.Raw("'RandomizedTaskVersion'"));
    </script>
}