@model CreateGroupViewModel

@{
    ViewData["Title"] = "Create Task Version Group";
}

@Html.EditorForModel()

@section Scripts
{
    <script>
        const viewModel = @Html.Raw(Json.Serialize(Model)); 
        const createView = new TaskVersionGroupView(viewModel);
    </script>
}