@model CounterBalancedVersionViewModel

@{
    ViewData["Title"] = "Edit Counter-Balanced Task Version";
}

@Html.EditorForModel()

@section Scripts
{
    <script>
        const viewModel = @Html.Raw(Json.Serialize(Model)); 
        const editView = new TaskVersionView(viewModel, @Html.Raw("'CounterBalancedTaskVersion'"));
    </script>
}