@model CounterBalancedVersionViewModel

@{
    ViewData["Title"] = "Create Counter-Balanced Task Version";
}

@Html.EditorForModel()

@section Scripts
{
    <script>
        const viewModel = @Html.Raw(Json.Serialize(Model)); 
        const createView = new TaskVersionView(viewModel, @Html.Raw("'CounterBalancedTaskVersion'"));
    </script>
}