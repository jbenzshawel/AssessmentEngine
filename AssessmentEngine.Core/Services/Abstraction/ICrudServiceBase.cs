using System.Threading.Tasks;
using AssessmentEngine.Domain;

namespace AssessmentEngine.Core.Services.Abstraction
{
    public interface ICrudServiceBase
    {
        // void DeleteEntity<TEntity>(TEntity entity);
        // void CreateEntity<TEntity>(TEntity entity) where TEntity : EntityBase;
        // void UpdateEntity<TEntity>(TEntity entity);
        // Task SaveEntityAsync<TEntity>(TEntity entity) where TEntity : EntityBase;
        // void SaveChanges();
        // Task SaveChangesAsync();
        // void SaveChanges(bool acceptAllChanges);
        // Task SaveChangesAsync(bool acceptAllChanges);
    }
}